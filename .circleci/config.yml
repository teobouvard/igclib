version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build:
    executor:
      tag: 3.6
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: |
            pytest
          name: Test

workflows:
  version: 2
  build:
    jobs:
      - build