version: 2.1

orbs:
  tester: thekevjames/tester@1.1.2
  python: circleci/python@0.2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - python/load-cache
      - run:
          name: Update pip
          command: pip install --user --upgrade pip
      - run:
          name: Install package
          command: pip install --user .
      - python/save-cache
      - tester/pytest

workflows:
  main:
    jobs:
      - build